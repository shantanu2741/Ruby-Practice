words = ["leetcode","win","loops","success"]
 pref = "code"
def prefix_count(words1, pref1)
    count=0
    words1.each do |word|
        if word.start_with?(pref1)
            count +=1
        end 
    end  
    puts count
  
end
prefix_count(words, pref)
